# ---- Python (backend) ----
backend/.venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Node (frontend) ----
node_modules/
frontend/dist/
frontend/dist-ssr/
*.local

# ---- Traktor data ----
# The collection is personal, large, and irreplaceable — don't commit it.
# Remove this if you deliberately want to version a sample .nml.
*.nml
# Legacy pre-git backups (superseded by version history; left ignored so any
# old .bak files on disk stay out of git).
*.nml.bak
*.nml.backup
*.nml.*.bak
backups/
# Version-history git repos, if a dev points KONDUKTOR_DATA_DIR inside the repo.
history/

# ---- Editor / OS ----
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# ---- Logs / env ----
*.log
.env
.env.*

# Local user preferences (not shared)
backend/userprefs.json

# ---- Desktop build (PyInstaller sidecar) ----
# The frozen binary + intermediate work. The spec, sidecar.py and
# build_sidecar.sh are source and ARE tracked.
backend/build/
backend/dist/
